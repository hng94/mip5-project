export enum SortEnum {
  COMMENT_COUNT = "commentCount",
  LIKE_COUNT = "likeCount",
}

export enum OrderStatus {
  Process = "Process",
  Shipping = "Shipping",
  Delivered = "Delivered",
  Canceled = "Canceled",
}
